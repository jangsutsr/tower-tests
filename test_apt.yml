---
- name: Test apt
  hosts: all
 
  tasks:
  - name: Remove if exist
      repo: 'deb http://dl.google.com/linux/chrome/deb/ stable main' 
      state: absent

  - name: Test apt 
    apt_repository: 
      repo: 'deb http://dl.google.com/linux/chrome/deb/ stable main' 
      mode: '0644'
      state: present
    sudo: yes
